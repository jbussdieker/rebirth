<%= form_for(@transaction_method) do |f| %>
  <% if @transaction_method.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@transaction_method.errors.count, "error") %> prohibited this transaction_method from being saved:</h2>

      <ul>
      <% @transaction_method.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :caption %><br />
    <%= f.text_field :caption %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
